[project]
name = "byoh"
version = "0.5.0"
description = "Build Your Own Harness — thinnest possible wrapper around LLM calls"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27.0"]

[project.scripts]
byoh = "byoh.cli:main"
